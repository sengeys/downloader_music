flutter clean

flutter pub get

dart run flutter_launcher_icons

flutter build windows --release

// Command Push git
git add .
git commit -m "Fix duplicate upload steps in Windows build"
git push

git tag v1.1.0
git push origin v1.1.0